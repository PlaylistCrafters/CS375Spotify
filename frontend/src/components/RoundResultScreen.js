const RoundResultsScreen = ({ roundResult }) => {
  // TODO display current leader board and also indicate if the current user got the answer correct
  return <div>Round Results Screen</div>;
};

export default RoundResultsScreen;
